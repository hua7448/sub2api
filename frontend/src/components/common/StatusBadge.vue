<template>
  <div class="flex items-center gap-1.5">
    <span
      :class="[
        'inline-block h-2 w-2 rounded-full',
        variantClass
      ]"
    ></span>
    <span class="text-sm text-txt-primary">
      {{ label }}
    </span>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'

const props = defineProps<{
  status: string
  label: string
}>()

const variantClass = computed(() => {
  switch (props.status) {
    case 'active':
    case 'success':
      return 'bg-neon-green/100'
    case 'disabled':
    case 'inactive':
    case 'warning':
      return 'bg-neon-amber/100'
    case 'error':
    case 'danger':
      return 'bg-neon-pink/100'
    default:
      return 'bg-gray-400'
  }
})
</script>
