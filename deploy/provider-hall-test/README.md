# Sub2API Provider Hall Test

Updated: 2026-09-15. Initialized, administrator login verified, all three containers healthy; loopback gateway origin override enabled for this isolated test instance.

## Release and Runtime

- Release: `0.1.179 + provider-hall`, supplied archive dated `20260912`.
- Source artifact: `/root/sub2apiAIHUB_TEST/sub2api-provider-hall-0.1.179-20260912.tar.gz`.
- Deployment packages the supplied embedded Linux amd64 binary; it is not a source rebuild.
- Binary SHA-256: `e5745db9f25404dc1db9aaf51259e276a65af4dec241e93ecbc7c732d4aafaa2`, verified against the archive checksum.
- App image: `sub2api-provider-hall:0.1.179-20260912`, ID `sha256:9e9415d6e84be06b407895654f0aa04db15c059c40b374522d14c87316c998e0`.
- PostgreSQL: `18.6` (`postgres:18-alpine`); Redis: `7.4.11` (`redis:7-alpine`).
- Docker Engine `29.1.3`, Compose `2.40.3`; units `docker.service`, `containerd.service`.
- Compose file: `/opt/sub2api-provider-hall-test/compose.yaml`; project `provider-hall-test`.
- Containers: `sub2api-provider-hall-test`, `sub2api-provider-hall-test-postgres`, `sub2api-provider-hall-test-redis`.
- Restart policy: `unless-stopped`, with dependency health checks on Compose startup.
- Role: `primary`; instance ID: `provider-hall-test`; timezone: `America/Los_Angeles`.
- No dedicated application systemd unit or Nginx vhost. Existing production units/vhosts were not edited.

## Network and Data Isolation

- Login URL on the server: `http://179.255.156.111:18082/login`, mapped to app port `8080`.
- No public domain configured. From the operator computer, keep this SSH tunnel open:

```sh
ssh -N -L 18082:127.0.0.1:18082 root@179.255.156.111
```

Then open `http://179.255.156.111:18082/login` locally.

- Docker network: `provider-hall-test_default`. PostgreSQL and Redis have no host-published ports.
- PostgreSQL host/database/user: `postgres:5432` / `sub2api_test` / `sub2api_test`.
- PostgreSQL external Docker volume: `sub2api-provider-hall-test-pgdata`, mounted at `/var/lib/postgresql` (PostgreSQL 18 layout).
- Redis host: `redis:6379`, DB `0`; AOF volume: `sub2api-provider-hall-test-redisdata`.
- App persistent bind: `/opt/sub2api-provider-hall-test/data` -> `/app/data`.
- Production database/Redis tunnels and production configuration files are not used or mounted.

## Credentials and Initialization

- Current administrator login: `/opt/sub2api-provider-hall-test/credentials/login.json`, mode `0600`, parent `0700`.
- The login identity is a random email-form identifier and its password is randomly generated. The test email is not a receiving mailbox.
- Database password: `credentials/db-password`. Runtime database password, administrator bootstrap values, fixed JWT secret and fixed TOTP key: `credentials/runtime.env` (mode `0600`).
- `data/config.yaml` is generated by the real auto-setup flow and contains secrets; keep it `0600`. `data/.installed` is created by setup, not copied from production.
- `AUTO_SETUP=true` completes first-run setup only when no installed configuration exists. Do not delete config or installation state to reset passwords.
- `credentials/login.json` is the verified current login record. The earlier unused administrator files were archived under `backups/failed-bootstrap-20260912/` and are not valid logins.
- Do not print tokens or copy secret values into logs, Git, issue text or this documentation.

## Operations and Verification

```sh
docker compose -f /opt/sub2api-provider-hall-test/compose.yaml ps
docker compose -f /opt/sub2api-provider-hall-test/compose.yaml up -d --wait
node /opt/sub2api-provider-hall-test/verify-login.cjs
/opt/sub2api-provider-hall-test/backup.sh
```

Environment changes require container recreation with `compose up -d`; `docker restart` does not reload an env file.

- `GET /setup/status` reports `needs_setup=false`, `step=completed`.
- `GET /health` succeeds. Login API, administrator profile and six frontend JS/CSS assets were verified before and after restarting all containers.
- `schema_migrations` confirms `231_provider_hall_facts.sql`, `232_provider_hall_metrics.sql`, `233_provider_hall_jobs.sql`; 17 provider-hall tables exist.
- Database has exactly one initialized administrator. No production users, channels or balances were imported.
- Provider-hall collection and display are enabled for this isolated test instance. Active scheduled tasks are currently paused after manual sample generation on 2026-09-16 to prevent continued test spend; manual probes require temporarily enabling tasks. The current loopback gateway origin is `http://127.0.0.1:8080`, which is reachable from inside the app container. `PROVIDER_HALL_ALLOW_LOOPBACK=1` is enabled so this loopback origin is accepted during local testing. For a public production-like origin, configure a real HTTPS domain without any path.
- The provider-hall operator user and probe API key were funded with test quota on 2026-09-15 after probe jobs returned `INSUFFICIENT_BALANCE`. Keep the operator balance and probe key quota positive before launching probes or verifications.
- On 2026-09-16, ordinary group `2` user traffic was generated with streaming requests to populate user-facing hall metrics. The hall showed real-user `success_rate`, `cache_rate`, `ttft_fast95_ms` and `ttft_p90_ms` after the TTFT sample count passed the threshold. Active scheduled tasks may create new probe/verification results after that point.

## Backups and Recovery

- Backups: `/opt/sub2api-provider-hall-test/backups`, mode `0700`.
- Empty isolated database backup before initialization: `test-before-initialize-20260912.dump`.
- Verified initialized backup: `20260912T150044Z/database.dump`; credentials/config archive and `SHA256SUMS` are alongside it. Files are `0600`.
- The initialized dump was restored into a temporary isolated database; all three provider-hall migrations and the administrator were verified. The temporary verification database was then dropped.
- `backup.sh` creates a custom-format database dump and archives configuration and credentials. It checks the dump directory with `pg_restore --list` and produces SHA-256 checksums. Backups are local, root-only and not off-host encrypted copies; no periodic timer is configured.
- Keep the credentials/config archive with the corresponding database backup, especially JWT/TOTP secrets.
- For recovery, stop only the test app, restore a chosen dump using PostgreSQL 18 `pg_restore` into a new isolated database, point the app to that database, restore matching configuration/secrets, then recreate the app and verify login. Never restore into the production tunnel.
- Stop the test stack with `docker compose -f /opt/sub2api-provider-hall-test/compose.yaml stop`. Do not remove persistent volumes as part of ordinary restarts.
- The old zero-byte production backup attempt is archived as `failed-bootstrap-20260912/production-backup.failed-empty`; it is not a valid backup.

Full operation history: `/root/.codex/deployments/sub2api-provider-hall-test/DEPLOYMENT-RECORD.md`.
